/* Modify the Login component so that the "login" button background color is "red" when the inputted password's length is shorter than 8 characters, green otherwise. */

import { Login } from "./Exercise62-Login";

export function App(){
    return <Login/>
}