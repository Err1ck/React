/* Create a Login component containing three inputs: 
a username input, a password input and a remember checkbox. 
All three inputs should be controlled components. */

import { Loggin } from "./Exercise42-Loggin";

export function App(){
    return <Loggin/>
}